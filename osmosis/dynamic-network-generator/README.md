# Dynamic Network Generator

This small script will take in an `input.yml` that defines the network topology of some networks,
and it will generate a docker-compose where the containers belong to networks that represent
the desired topology.